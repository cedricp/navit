set(SPEEDCAM_MAP_NAME speedcamfr.csv)

file(COPY ${SPEEDCAM_MAP_NAME}
	DESTINATION ${CMAKE_CURRENT_BINARY_DIR}/../maps)

install(FILES ${SPEEDCAM_MAP_NAME}
	DESTINATION ${SHARE_DIR}/maps
	PERMISSIONS OWNER_READ OWNER_WRITE GROUP_READ WORLD_READ)
	